# 内容管理功能检查

| 内容 | 新增 | 删除 | 修改 | 查找 |
| --- | --- | --- | --- | --- |
| 课程 | 首页、侧栏添加课程 | 首页卡片、课程顶部、课程设置 | 课程设置中的名称、编号、教师、学期、考试日期 | 首页按名称、编号、教师、学期搜索 |
| 章节 | 章节与课程设置中添加 | 章节行的删除按钮 | 章节行的改名按钮 | 章节名称搜索 |
| 资料 | 上传 PDF、DOCX、TXT、Markdown | 资料列表中的移除按钮 | 重命名、修改所属章节、重新解析 | 名称、正文、章节检索及章节筛选 |
| 笔记 | 知识库新建、问答摘录 | 笔记详情中的删除笔记 | 编辑标题、正文、标签、章节、复习问题等 | 标题、正文、标签、章节检索及课程筛选 |
| 学习任务 | 复习页添加任务 | 任务行删除按钮 | 展开任务后编辑任务信息：名称、内容、类型、课程、日期；可切换完成状态 | 名称、内容、课程、日期、类型、状态搜索 |
| 对话 | 问答页新建对话 | 对话顶部删除对话 | 对话顶部改名 | 对话列表按标题、消息正文搜索 |
| 对话消息 | 输入问题并发送，生成回答 | 消息下方删除消息 | 消息下方编辑消息 | 所在对话的全文搜索 |
| 篇内概念 | 手动绘制或生成图谱后添加概念 | 编辑概念时删除非中心节点 | 概念名称、解释及关系编辑 | 搜索概念名称、解释 |
| 笔记关联 | 图谱或笔记详情保存关联 | 全局图谱移除关联、笔记详情解除关联 | 重新选择关系并保存 | 全局笔记搜索、关联笔记搜索、查看选中节点的已有关系 |

设置与备份属于单份配置，可查看和修改；统计数字、引用片段、复习进度等由课程、资料和笔记派生，不另建重复的数据管理入口。原始上传文件不在网页中改写，资料管理支持名称和分类修改。

## 删除与保存规则

- 课程、笔记、章节、对话、消息、任务和资料的删除均有确认弹窗。
- 删除课程同时移除课程资料列表、笔记、对话、学习任务，并清理相关笔记关联；上传原文件保留，避免其他内容的引用链接失效。
- 删除章节保留资料和笔记，将其归入“未分类”；改名同步更新两者的章节。
- 删除对话或消息保留已经摘录的独立笔记；修改消息不会自动重写已有回答。
- 删除笔记同时清理其他笔记指向它的关联，退出当前复习队列。
- 移除全局图谱关联会清除这两条笔记之间的双向关联；可随时重新添加。
- 允许删除所有课程，空知识库也会保存，刷新后不会恢复默认课程。
- 所有修改沿用现有自动保存机制；连接失败或发生多窗口冲突时，页面仍会显示原有的保存错误提示。

## 回归检查

`tools/crud-smoke.cjs` 用独立浏览器上下文和内存 API 响应验证内容管理流程，不写入真实知识库。需要本机 Edge、运行中的 `http://localhost:3000/` 和 `playwright-core`，可通过 `PLAYWRIGHT_MODULE` 指定模块路径，通过 `EDGE_PATH` 指定浏览器路径。

静态检查：`node node_modules/typescript/bin/tsc --noEmit --incremental false`，以及 `npm run lint`。

类型检查通过。本次修改的页面、图谱组件和回归脚本通过定向 lint；全项目 lint 仍存在原有通用 UI 组件及 `hooks/use-mobile.ts` 的 19 条报错（无障碍语义、React effect、图表模板类型），这些文件未在本次内容管理功能修改中调整。
